# lang
export LANG=ja_JP.UTF-8

# color
autoload -Uz colors
colors

# auto
autoload -Uz compinit
compinit

# history
HISTFILE=~/.zsh_history
HISTSIZE=1000000
SAVEHIST=1000000

# ディレクトリを指定しただけでそのディレクトリに移動するようになる
setopt AUTO_CD